import React        from "react";

export const $NEW = React.createContext();

export const with$NEW = (Component) => {
  return (
    (props) => {
      return (
        <$NEW.Consumer>
          {context => <Component {...props} g={context} />}
        </$NEW.Consumer>
      );
    }
  );
};
